
<%= render 'books/list', book: @book %>

<div>
  <h2>Users</h2>
  <% @users.each do |user| %>
  <div>
    <p>image</p>
    <%= image_tag user.get_profile_image(100,100) %>
    <p>name</p>
      <%= user.name %>
    <%= link_to "Show", user_path(user.id) %>
  </div>
  <% end %>
</div>
